package Utils;

import Representations.Map;
import Representations.Graph;

public class GridGraphCreator implements iGraphCreator{
	public Graph createGraph (Map map) {
		return new Graph(); // dummy, for now.
	}
}

