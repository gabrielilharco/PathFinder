package utils;

import representations.Graph;
import representations.maps.Map;

public class WaypointGraphCreator implements IGraphCreator{
	public Graph createGraph (Map map) {
		return new Graph(); // dummy, for now.
	}
}

